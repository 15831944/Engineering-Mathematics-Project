/*#include "Interpreter.h"
#include "Vector.h"
#include "Matrix.h"
map<string, VAR> Interpreter::vars;

template<T>
VAR loadSingleVar(ifstream& fin) {
	VAR var;
	int n;
	fin >> n;
	initializer_list<NumType> list;

}

vector<ListViewItem^> LoadVars(string path) {
	ifstream fin(path);
	int n,count=0;
	string type;
	fin >> n;
	for (int i = 0; i < n; ++i) {
		

	}
}*/